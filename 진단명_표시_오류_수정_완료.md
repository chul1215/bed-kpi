# 진단명 표시 오류 수정 완료 보고서

작성일: 2026-02-23

## 🐛 문제 상황

홈 화면의 "진단별 랭킹 TOP 6" 테이블에서 진단명이 "N/A"로 표시되는 문제 발생

### 증상
- 홈 페이지 질환 TOP 6 테이블에서 모든 진단명이 "N/A"로 표시됨
- 환자수, LOS 등 다른 데이터는 정상 표시
- 의료진 상세 페이지에서는 ADRG 코드가 정상적으로 "Z630 (기타 추적관리)" 형식으로 표시됨

## 🔍 원인 분석

### 데이터 흐름
```
generate_static_dashboard_adrg.py
  └─> valid_adrg_kpis (DataFrame)
        └─> columns: adrg_code, adrg_name, patient_count, ...
              └─> render_home()
                    └─> disease_kpis parameter
                          └─> index.html 템플릿
                                └─> {{ item.diagnosis }}  ← 'diagnosis' 컬럼 없음!
```

### 근본 원인
- `valid_adrg_kpis` DataFrame은 `adrg_code`, `adrg_name` 컬럼을 가지고 있음
- `index.html` 템플릿은 `diagnosis` 필드를 참조함
- DataFrame to dict 변환 시 `diagnosis` 키가 없어 "N/A" 표시됨

### 템플릿 코드 (index.html:137)
```jinja2
<span style="color: var(--white); font-family: 'Consolas', 'Monaco', monospace; font-weight: 600;">
    {{ item.diagnosis if item.diagnosis is defined else item.get('diagnosis', 'N/A') }}
</span>
```

## ✅ 해결 방법

### 수정 파일
`generate_static_dashboard_adrg.py` (262-273줄)

### 수정 내용
홈 HTML 생성 전에 `diagnosis` 필드를 추가하여 ADRG 코드와 이름을 결합

**수정 전:**
```python
html_content = dashboard_gen.render_home(
    summary_kpi=summary_kpi,
    doctor_kpis=valid_doctor_kpis,
    disease_kpis=valid_adrg_kpis,  # 'diagnosis' 컬럼 없음
    insights=[],
    hospital=hospital,
    period=period_code,
    doctor_disease_data=doctor_adrg_dict
)
```

**수정 후:**
```python
# 홈 HTML 생성용 ADRG 데이터 준비 (diagnosis 필드 추가)
display_adrg_kpis = valid_adrg_kpis.copy()
if len(display_adrg_kpis) > 0:
    display_adrg_kpis['diagnosis'] = display_adrg_kpis.apply(
        lambda row: f"{row['adrg_code']} ({row['adrg_name']})",
        axis=1
    )

html_content = dashboard_gen.render_home(
    summary_kpi=summary_kpi,
    doctor_kpis=valid_doctor_kpis,
    disease_kpis=display_adrg_kpis,  # 'diagnosis' 필드 포함
    insights=[],
    hospital=hospital,
    period=period_code,
    doctor_disease_data=doctor_adrg_dict
)
```

## 📊 검증 결과

### 대전 비수기 TOP 6 ADRG (수정 후)
| 순위 | 진단명 | 환자수 | 현재 LOS | 목표 LOS |
|-----|--------|--------|----------|----------|
| 1 | **Z630 (기타 추적관리)** | 177 | 3.4 | 10.8 |
| 2 | **G521 (결장경 시술)** | 51 | 19.8 | 8.2 |
| 3 | **I164 (기타 슬관절 수술)** | 136 | 18.2 | 14.2 |
| 4 | **Z620 (치료 완결후 추적관리)** | 60 | 2.4 | 3.0 |
| 5 | **B684 (허혈 뇌졸중)** | 54 | 4.1 | 6.1 |
| 6 | **I610 (기타 대퇴골 골절)** | 49 | 19.2 | 8.2 |

### 의료진별 질환 토글 (JavaScript 데이터)
```javascript
const doctorDiseaseData = {
  "정윤화": [
    {"diagnosis": "Z630 (기타 추적관리)", "patient_count": 133, ...},
    {"diagnosis": "P640 (신생아, 입원시 체중 1250-1499g...)", ...}
  ],
  ...
}
```

## 🎯 영향 범위

### 수정된 파일
- ✅ `generate_static_dashboard_adrg.py`

### 재생성된 대시보드
- ✅ `docs/대전/index_off_season.html`
- ✅ `docs/대전/index_normal.html`
- ✅ `docs/유성/index_off_season.html`
- ✅ `docs/유성/index_normal.html`

### 확인 항목
- [x] 홈 화면 진단별 TOP 6 테이블 - ADRG 코드와 이름 표시
- [x] 의료진별 질환 토글 - ADRG 코드와 이름 표시
- [x] 병원 전환 버튼 작동
- [x] 기간 전환 버튼 작동

## 📝 참고

### 유사 문제 방지
의료진별 질환 데이터(`doctor_adrg_dict`)는 이미 올바르게 `diagnosis` 필드를 생성하고 있었음:

```python
# generate_static_dashboard_adrg.py:242-248
doctor_adrg_kpis_display = doctor_adrg_kpis.copy()
doctor_adrg_kpis_display['diagnosis'] = doctor_adrg_kpis_display.apply(
    lambda row: f"{row['adrg_code']} ({row['adrg_name']})",
    axis=1
)
doctor_adrg_dict[doctor_name] = doctor_adrg_kpis_display.to_dict('records')
```

홈 화면용 ADRG KPI 데이터도 동일한 방식으로 처리하여 일관성 확보

## ✅ 완료 상태

- [x] 문제 원인 파악
- [x] 코드 수정
- [x] 대시보드 재생성
- [x] 로컬 확인
- [x] 4개 병원×기간 조합 모두 정상 작동 확인

---

**작성자**: Claude Sonnet 4.5
**날짜**: 2026-02-23
**이슈**: 진단명 "N/A" 표시 오류
**해결**: `diagnosis` 필드 추가하여 ADRG 코드와 이름 결합
