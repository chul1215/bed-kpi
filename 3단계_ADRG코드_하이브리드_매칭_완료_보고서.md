# 3단계: ADRG 코드 기반 하이브리드 매칭 완료 보고서

**작업일자**: 2026-02-23
**작업자**: Claude Code

---

## 📋 작업 개요

병원에서 제공한 새 SMC 파일(`25년도_대전_유성_의사별_퇴원진단_KDRG46_ADRG추가.xlsx`)에 **ADRG 코드 컬럼**이 추가되어, 기존 진단명 기반 매칭에서 **ADRG 코드 우선 + 진단명 보조** 하이브리드 매칭으로 업그레이드하였습니다.

---

## 🎯 작업 목표

1. ✅ **ADRG 코드 기반 직접 매칭 구현** (최우선)
2. ✅ **진단명 기반 매칭 유지** (폴백)
3. ✅ **매칭률 향상** (96.3% → 98%+)
4. ✅ **정적 HTML 대시보드 재생성**

---

## 📊 최종 매칭률

### 전체 환자 매칭률

| 병원 | 전체 환자 | ADRG 코드 매칭 | 진단명 매칭 | 총 매칭률 |
|------|-----------|----------------|-------------|-----------|
| **대전선병원** | 15,514명 | 11,917명 (76.8%) | 3,235명 (20.9%) | **97.7%** ✅ |
| **유성선병원** | 18,339명 | 13,250명 (72.3%) | 4,831명 (26.3%) | **98.6%** ✅ |
| **합계** | 33,853명 | 25,167명 (74.3%) | 8,066명 (23.8%) | **98.1%** ✅ |

### 개선 효과

- **기존 진단명 기반**: 96.3% (32,655명 / 33,853명)
- **ADRG 하이브리드**: **98.1%** (33,233명 / 33,853명)
- **개선폭**: **+1.8%p** (578명 추가 매칭)

---

## 🔧 구현 내용

### 1. DRGMatcher 하이브리드 로직 구현

**파일**: `backend/app/services/drg_matcher.py`

#### 매칭 우선순위

```python
def get_target_los(diagnosis: str, adrg_code: str | None = None) -> float | None:
    """
    매칭 우선순위:
    1. ADRG 코드 직접 매칭 (가장 정확) ← 최우선
    2. 진단명 수동 매핑 (diagnosis_kdrg44_mapping.xlsx)
    3. 진단명 = ADRG명 직접 일치
    4. 진단명 부분 일치
    """
    # 1. ADRG 코드 직접 매칭 (최우선)
    if adrg_code and str(adrg_code) != '미매핑':
        adrg_3 = adrg_code[:3] if len(adrg_code) >= 3 else adrg_code
        if adrg_3 in self.adrg_code_to_los:
            return self.adrg_code_to_los[adrg_3]

    # 2-4. 진단명 기반 폴백 (기존 로직)
    ...
```

#### 주요 변경사항

1. **`adrg_code_to_los` 딕셔너리 추가**
   - ADRG 3자리 코드 → 목표 LOS 매핑
   - HIRA 4분기 데이터 기준 391개 ADRG 커버

2. **`load_hira_adrg_los()` 업데이트**
   - 기존: ADRG명 → LOS만 매핑
   - 변경: ADRG명 + ADRG 코드 모두 매핑

3. **`match_smc_to_hira()` 업데이트**
   - ADRG 코드 우선 매칭 통계 추가
   - 진단명별 가장 많이 나타나는 ADRG 코드 사용

### 2. FileParser ADRG 컬럼 파싱

**파일**: `backend/app/services/file_parser.py`

#### 변경사항

```python
# ADRG 컬럼 자동 감지
has_adrg_col = 'ADRG' in df.columns

# ADRG 컬럼 정제
if has_adrg_col:
    df['ADRG'] = df['ADRG'].astype(str).str.strip()
    logger.info(f"ADRG 컬럼 발견: {df['ADRG'].nunique()}개 고유 코드")

# adrg_code로 컬럼명 변경
if has_adrg_col and 'ADRG' in df.columns:
    df = df.rename(columns={'ADRG': 'adrg_code'})
```

### 3. 설정 파일 업데이트

**파일**: `backend/app/config.py`

```python
# 새 SMC 파일 경로 (ADRG 코드 포함)
PRELOADED_SMC_FILE: Path = PROJECT_ROOT / "data/smc/25년도_대전_유성_의사별_퇴원진단_KDRG46_ADRG추가.xlsx"
```

---

## 📈 매칭 통계 상세

### SMC 파일 ADRG 코드 현황

- **전체 환자**: 33,853명
- **ADRG 코드 있음**: 31,602명 (93.4%)
- **미매핑**: 2,251명 (6.6%)
- **고유 ADRG 코드**: 189개

### HIRA 파일 ADRG 코드 현황

- **전체 ADRG**: 739개 (4분기만)
- **ADRG 3자리 코드**: 391개

### ADRG 코드 매칭 커버리지

| 구분 | SMC 고유 코드 | HIRA 고유 코드 | 교집합 | 매칭률 |
|------|---------------|----------------|--------|--------|
| **3자리 ADRG** | 152개 | 391개 | 121개 | 79.6% |

**참고**: HIRA가 4분기 데이터만 있어서 1~3분기 환자의 일부 ADRG 코드가 HIRA에 없음 (예: A09, B09, D73 등)

---

## 🎨 정적 HTML 대시보드 재생성

### 생성 결과

```
✅ 대전선병원 HTML 생성 완료
  - 비수기 (3-4·11-12월): 2,557명
  - 통상기간 (1-2·5-10월): 1,183명

✅ 유성선병원 HTML 생성 완료
  - 비수기: 2,997명
  - 통상기간: 1,326명

📁 출력: docs/ 폴더 (88개 HTML 파일)
```

### 파일 구조

```
docs/
├── index.html (리디렉션)
├── README.md
├── 대전/
│   ├── index_off_season.html
│   ├── index_normal.html
│   ├── department_off_season.html
│   ├── department_normal.html
│   ├── doctors_off_season/*.html (10명)
│   ├── doctors_normal/*.html (10명)
│   ├── diseases_off_season/*.html (10개)
│   └── diseases_normal/*.html (10개)
└── 유성/ (동일 구조)
```

---

## 🔍 미매칭 진단명 분석 (TOP 10)

| 진단명 | 환자수 | ADRG 코드 | 미매칭 원인 |
|--------|--------|-----------|-------------|
| 이식장치의 조정 및 관리를 위하여 보건서비스와 접하고 있는 사람 | 23명 | F800 | HIRA 4분기에 없음 |
| 동맥 및 세동맥의 기타 장애 | 21명 | F181 | HIRA 4분기에 없음 |
| 급성 A형간염 | 19명 | H661 | HIRA 4분기에 없음 |
| 다발성 및 상세불명 부위의 급성 상기도감염 | 16명 | D750 | HIRA 4분기에 없음 |
| 달리 분류되지 않은 간부전 | 15명 | H671 | HIRA 4분기에 없음 |
| 고관절의 관절 및 인대의 탈구, 염좌 및 긴장 | 14명 | I282 | HIRA 4분기에 없음 |
| 기능성 소화불량 | 12명 | 미매핑 | SMC 데이터에서 미매핑 |
| 인공개구에 대한 관리를 위하여 보건서비스와 접하고 있는 사람 | 12명 | E773 | HIRA 4분기에 없음 |
| 기타 내부 인공삽입장치, 삽입물 및 이식편의 합병증 | 11명 | B814 | HIRA 4분기에 없음 |
| 직장구불결장접합부의 악성 신생물 | 10명 | 미매핑 | SMC 데이터에서 미매핑 |

**해결 방안**: HIRA 연간 데이터 확보 시 추가 개선 가능 (예상 99%+ 매칭률)

---

## 📁 관련 파일

### 수정된 파일

1. **`backend/app/services/drg_matcher.py`** ← 핵심 변경
   - ADRG 코드 기반 하이브리드 매칭 로직 추가

2. **`backend/app/services/file_parser.py`**
   - ADRG 컬럼 자동 파싱 지원

3. **`backend/app/config.py`**
   - 새 SMC 파일 경로 업데이트

### 새로 생성된 파일

1. **`test_adrg_matching.py`**
   - ADRG 하이브리드 매칭 검증 스크립트

2. **`3단계_ADRG코드_하이브리드_매칭_완료_보고서.md`** (현재 문서)

### 데이터 파일

- **입력**: `data/smc/25년도_대전_유성_의사별_퇴원진단_KDRG46_ADRG추가.xlsx`
- **출력**: `docs/` 폴더 (88개 HTML 파일)

---

## 🚀 실행 방법

### 1. 매칭 테스트

```bash
python3 test_adrg_matching.py
```

**예상 출력**:
```
✅ 하이브리드 매칭 완료:
  - 대전: 97.7% (ADRG 76.8% + 진단명 20.9%)
  - 유성: 98.6% (ADRG 72.3% + 진단명 26.3%)
```

### 2. 정적 HTML 재생성

```bash
python3 generate_static_dashboard.py
```

**출력**: `docs/` 폴더에 88개 HTML 파일 생성

### 3. 로컬 확인

```bash
# 방법 1: 파일 직접 열기 (권장)
open docs/index.html

# 방법 2: 웹서버 실행 (선택)
cd docs && python3 -m http.server 8080
# http://localhost:8080 접속
```

---

## 📊 성과 요약

### 매칭률 개선 효과

| 단계 | 방식 | 매칭률 | 개선폭 |
|------|------|--------|--------|
| 1단계 | 진단명 수동 매핑 (164개) | 95.0% | - |
| 2단계 | ICD-10 자동 + 진단명 수동 | 96.3% | +1.3%p |
| **3단계** | **ADRG 코드 + 진단명 하이브리드** | **98.1%** | **+1.8%p** |

### 전체 개선폭 (1단계 → 3단계)

- **초기 (1단계)**: 95.0% (진단명 수동 164개)
- **최종 (3단계)**: **98.1%** (ADRG 코드 + 진단명)
- **총 개선**: **+3.1%p** (1,050명 추가 매칭)

### 매칭 방식별 기여도

- **ADRG 코드**: 74.3% (25,167명) ← **주력**
- **진단명 매핑**: 23.8% (8,066명) ← 보조

---

## 🎯 향후 개선 방안

### 1. HIRA 연간 데이터 확보 (권장) ⭐

**현재 문제**:
- HIRA 4분기 데이터만 있어서 1~3분기 환자의 ADRG 코드가 매칭 안 됨
- 31개 ADRG 코드 (약 620명)가 HIRA에 없음

**해결 시 효과**:
- 예상 매칭률: **99%+**
- 추가 매칭: 약 600명

### 2. SMC 미매핑 진단명 매핑 추가

**현재 상태**:
- SMC에서 "미매핑"으로 표시된 진단명: 2,251명 (6.6%)
- 주요 진단명: 기능성 소화불량, 직장구불결장접합부의 악성 신생물 등

**해결 방법**:
- 병원에 ADRG 코드 추가 요청
- 또는 수동 매핑 테이블에 추가

### 3. 데이터베이스 연동 (선택)

- 현재: 정적 HTML (파일 기반)
- 개선: SQLite/PostgreSQL 연동
- 효과: 동적 필터링, 엑셀 다운로드, 검색 기능 강화

---

## ✅ 결론

### 주요 성과

1. ✅ **ADRG 코드 기반 하이브리드 매칭** 구현 완료
2. ✅ **매칭률 98.1% 달성** (기존 96.3% 대비 +1.8%p)
3. ✅ **정적 HTML 대시보드** 재생성 완료 (88개 파일)
4. ✅ **전체 160명 의료진** 모두 데이터 표시 가능

### 기술적 개선

- **ADRG 코드 우선 매칭** (74.3% 커버)
- **진단명 폴백 매칭** (23.8% 커버)
- **하이브리드 전략**으로 최대 커버리지 달성

### 배포 준비 완료

- ✅ GitHub Pages 즉시 배포 가능
- ✅ 서버 없이 브라우저에서 직접 실행
- ✅ 병원/기간 전환 작동
- ✅ NaN 값 안전 처리

---

## 📞 문의

- **테스트 스크립트**: `test_adrg_matching.py`
- **생성 스크립트**: `generate_static_dashboard.py`
- **설정 파일**: `backend/app/config.py`
- **매칭 로직**: `backend/app/services/drg_matcher.py`

---

**작업 완료일**: 2026-02-23
**최종 매칭률**: 98.1% ✅
