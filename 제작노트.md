# top7_v7.html 제작노트

> 작성일: 2026-02-25
> 파일: `new/top7_v7.html`
> 목적: 대전·유성선병원 의사별 TOP7 진단 목표재원일수 단일 HTML 대시보드

---

## 1. 데이터 기준 확정 과정

### 1-1. 가동률 기준값 문제

초기 summary 값은 **전체 원본 데이터(필터 없음)** 기준이었음.

| 병원 | 초기 기준 | 최종 기준 |
|------|----------|----------|
| 대전 | 전체 15,514건 기준, 가동률 98.9% | 의사별 진단 n≥6, 가동률 82.0% |
| 유성 | 전체 18,339건 기준, 가동률 83.9% | 의사별 진단 n≥6 + FM제외, 가동률 72.1% |

### 1-2. n≥6 필터 기준 세 가지 비교

유성선병원 bedsimulator 사이트(`https://ysmc-bedsimulator.netlify.app/index.html`)를 기준으로 삼아 역산함.

| 필터 방식 | 환자수 | 병상일수 | 가동률 |
|----------|--------|---------|--------|
| 전체 (필터 없음) | 18,339명 | 120,185일 | 85.56% |
| 진단 전체 n≥6 | 17,815명 | 115,293일 | **82.08%** ← bedsimulator meta |
| **의사별 진단 n≥6** | **16,261명** | **102,883일** | **73.25%** ← bedsimulator 표시값 |
| 의사별 n≥6 + FM제외 | 15,362명 | 101,246일 | 72.08% |

→ **최종 채택: 의사별 진단 n≥6 + FM제외**
→ 유성 102,883일 / 140,462일 = **72.1%** (FM 제외 후 102,883→101,246일 변경)

### 1-3. 방향(direction) 변경

두 병원 모두 목표(88.3%) 미달 → **연장** 방향으로 통일

| 병원 | 변경 전 | 변경 후 | 이유 |
|------|--------|--------|------|
| 대전 | 단축 (98.9%) | **연장** (82.0%) | 의사별 n≥6 기준으로 재산출 시 목표 미달 |
| 유성 | 연장 (83.9%) | **연장** (72.1%) | 기준 변경으로 가동률 하락 |

---

## 2. 심평원 매칭 완성

### 2-1. 매칭 현황

| 병원 | 초기 매칭 | 최종 매칭 |
|------|----------|----------|
| 대전 | 205/255건 (80%) | **255/255건 (100%)** |
| 유성 | 89/293건 (30%) | **293/293건 (100%)** |

### 2-2. 매칭 방법 (3단계)

1. **상병명 직접 일치** — `진단명 == 상병명` (10개 진단)
2. **부분 문자열 일치** — `진단명 in 상병명` or `상병명 in 진단명` (54개)
3. **ICD-10 코드 수동 매핑** — 코드 범위 직접 지정 (46개)
   - 예외 처리: `급성 심근경색증` I232(95.0일 이상치) 제외, I21x만 사용 → 5.6일
   - 예외 처리: `자반 및 기타 출혈성 병태` D698 이상치 제외 → 4.9일

### 2-3. vs 심평원 부호 방향

- **저장값** `vs_hira = cur - hira` (유지)
- **표시값** = `hira - cur` = `-vs_hira` (표시 시 반전)
- 의미: "심평원 수준에 도달하기 위해 필요한 변화량"
  - 양수(+): 재원일수를 늘려야 심평원 도달
  - 음수(−): 재원일수를 줄여야 심평원 도달

---

## 3. 주요 기능 추가

### 3-1. 상단 요약카드 — 평균재원일수 표시

허가병상·현재가동률 카드에 전체 평균재원일수 추가:
```
계산: (cur_off_avg × n_off + cur_norm_avg × n_norm) / (n_off + n_norm)
```

### 3-2. 연간목표(88.3%) 컬럼 추가

비수기(85%) / 통상(90%) 외에 **연간 전체 기준 목표**를 별도 컬럼으로 표시:
```
annual_ratio = avail × tgt_occ% / cur_total
tgt_annual   = cur × annual_ratio
diff_annual  = cur - tgt_annual
```
- `window['annual_ratio_'+h]`에 저장, renderTbl()에서 참조
- 테이블 컬럼: 의사명 / 진단명 / 환자수 / 비수기목표 / 통상목표 / **연간목표** / 심평원 / vs심평원

### 3-3. 진료과별 드롭다운 그룹화

- `RAW.summary[h].doc_dept = {의사명: 진료과코드}` 추가
- 의사 선택 드롭다운에 `<optgroup label="진료과코드">` 적용
- 원본 Excel에서 의사별 최다 퇴원과를 주 진료과로 지정

### 3-4. 0.5일 필터 제거

- 기존: 비수기·통상 모두 diff < 0.5일이면 레코드 제외
- 변경: **필터 없음**, 조건 충족 시 전체 TOP7 진단 표시
- 레코드 수: 대전 206→255건, 유성 89→286건

---

## 4. 버그 수정 (디버깅)

| 버그 | 심각도 | 내용 | 수정 |
|------|--------|------|------|
| A | CRITICAL | `gp()` 페이지 이동 시 filt 배열 병원 교차 오염 | gp() 내 af(h) 선호출 추가 |
| B | CRITICAL | `sw()` 탭 전환 시 curPg 미초기화 → 빈 테이블 | curPg=1 리셋 + af(h) 호출 |
| C | MEDIUM | tgt_off/tgt_norm .toFixed(1) 누락 → 정수 표시 불일치 | .toFixed(1) 적용 |
| D | MEDIUM | vs_hira 색상이 방향 무관하게 적용 | direction 기반 overhira/underhira 분기 |

---

## 5. 최종 데이터 현황

| 항목 | 대전 | 유성 |
|------|------|------|
| 레코드 수 | 255건 | 286건 |
| 의사 수 | 43명 | 50명 |
| FM 제외 | ✓ | ✓ |
| 가동률 | 82.0% | 72.1% |
| 방향 | 연장 | 연장 |
| 비수기 현재/목표 | 7.71일 / 7.85일 | 6.56일 / 7.75일 |
| 통상 현재/목표 | 7.55일 / 8.36일 | 6.61일 / 8.12일 |
| 심평원 매칭 | 255/255 (100%) | 286/286 (100%) |
| 조정비율 비수기 | 1.0176 | 1.1820 |
| 조정비율 통상 | 1.1078 | 1.2280 |

---

## 6. 향후 과제

- [ ] 진단코드(ICD-10) 컬럼 추가
- [ ] CSV/Excel 내보내기
- [ ] 모바일 반응형 (현재 min-width: 900px)
- [ ] 라이트 모드
- [ ] 의사별 상세 드릴다운 (월별 추이 차트)
- [ ] 인쇄 최적화 CSS
- [ ] 데이터 외부 분리 (JSON 파일 또는 API)
